# Nuxt dev/build outputs
.output
.data
.nuxt
.nitro
.cache
.build
build
dist

# Regenerated on every `nuxt prepare`; content is tied to this machine's build cache
# (see nuxt.config.ts for why the tsconfig must be self-contained, not `extends`-based)
/tsconfig.json


# Node dependencies
/node_modules 

# Logs
logs
*.log

# Misc
.DS_Store
.fleet
.idea

# Local env files
.env
.env.*
!.env.example
# Other
/build
/dist-electron
/release
/vease-back*
/vease-viewer*
VTK.txt
/data
__pycache__
*.egg-info

# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
package-lock.json

# Compiled Node-safe siblings of tests/e2e/utils/ (see rolldown.config.ts)
/tests/e2e/utils/**/*.js
# Local Netlify folder
.netlify

extensions